@using DentalDesign.Dashboard.Models.User
@model UserEditViewModel

@{
    Layout = "_Layout";
    ViewBag.Title = "Edit Supervisor";
}

<!-- عرض الـ Partial داخل صفحة كاملة -->
@await Html.PartialAsync("_EditPartial", Model)

<!-- تحميل سكريبتات التحقق (لأنه صفحة كاملة) -->
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}